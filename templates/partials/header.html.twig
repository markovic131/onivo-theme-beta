<nav class="navbar navbar-default navbar-fixed-top" role="navigaion" id="header">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            </button>
            <a href="{{ base_url == '' ? '/' : base_url }}" class="navbar-brand">
                <img src="{{ url("theme://images/logo.png") }}" alt="{{ config.site.title }}">
            </a>
        </div>
        <div class="navbar-collapse collapse">
            <ul class="nav navbar-nav navbar-right" id="navigation">
                {% for page in pages.children %}
                    {% if page.visible %}
                        {% set current_page = (page.active or page.activeChild) ? 'active' : '' %}
                        <li class="{{ current_page }}"><a href="{{ page.url }}">{{ page.menu }}</a></li>
                    {% endif %}
                {% endfor %}
                <li id="divider" class="dividerVertical"></li>
                <li class="registerLink"><a href="https://app.onivo.rs/register">Registracija</a></li>
                <li class="loginLink"><a href="https://app.onivo.rs">Prijava</a></li>
            </ul>
        </div>
    </div>
</nav>
